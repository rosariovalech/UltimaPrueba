<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @company.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @company.content %>
</p>


<h3>Reclamos</h3>
 
<% if @company.claims.any? %>
  <ul>
    <% @company.claims.each do |claim| %>
    <li><%= link_to claim, claim %></li> 
    <% end %>
  </ul>
<% else %>
  <p>no hay reclamos</p>
<% end %>


<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
