$(document).on('turbolinks:load', function(){
	$('[name="company[name]"]').on('change', function(event){
		var company_id = $(this).val();
		$.ajax({
			url: '/claims/index',
			type: 'GET',
			dataType: 'script',
			data: {company_id: company_id}
		})
	});
});